// returns associated token account public key (address) given normal public key
// gives address even if account doesn't exist, it's a one to one function I think
// const toAssociatedTokenAddress = await Token.getAssociatedTokenAddress(
//   token.associatedProgramId,
//   token.programId,
//   token.publicKey,
//   Keypair.generate().publicKey
// )

// works if publickey already has an associated token account
// gives account info even if account doesn't exist, it's also a one to one function I think
// const fromTokenAccount = await token.getOrCreateAssociatedAccountInfo(new PublicKey(fromPublicKey))
// const fromAssociatedTokenAddress = fromTokenAccount.address



// if associated token account doesn't exist
// creates associated token account for owner with associated token address 
// const accountCreationInstruction = Token.createAssociatedTokenAccountInstruction(
//   token.associatedProgramId,
//   token.programId,
//   mintPublicKey,
//   toAssociatedTokenAddress,
//   toPublicKey,
//   fromPublicKey
// )